{
  "name": "homepage",
  "version": "1.1.0",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "build:preview": "vite build; vite preview",
    "clean": "rm -rf dist"
  },
  "devDependencies": {
    "vite": "^2.7.0"
  },
  "dependencies": {
    "three": "^0.135.0"
  }
}
